import { StyledCatalogueContainer } from "./styles";

const CatalogueContainer = ({ children }) => {
  return <StyledCatalogueContainer>{children}</StyledCatalogueContainer>;
};

export default CatalogueContainer;
